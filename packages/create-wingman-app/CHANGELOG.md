# create-wingman-app

## 0.2.0

### Minor Changes

- e7a8895: Implement project scaffolding — `npx create-wingman-app my-app` now generates a working Wingman chat application with `package.json`, `src/server.ts`, `tsconfig.json`, and `.gitignore`.
